const nodemailer=require("nodemailer");module.exports=async(req,res)=>{if(req.method!=="POST")return res.status(405).end();try{const b=req.body||{};const tr=nodemailer.createTransport({host:process.env.SMTP_HOST,port:Number(process.env.SMTP_PORT||465),secure:true,auth:{user:process.env.SMTP_USER,pass:process.env.SMTP_PASS}});await tr.sendMail({from:process.env.SMTP_USER,to:process.env.CONTACT_EMAIL||process.env.SMTP_USER,replyTo:b.email,subject:"Nueva consulta BoscTech",text:`Nombre: ${b.nombre}\nTeléfono: ${b.telefono}\nEmail: ${b.email}\nEquipo: ${b.equipo}\n\n${b.mensaje}`});res.status(200).json({ok:true})}catch(e){console.error(e);res.status(500).json({ok:false})}};